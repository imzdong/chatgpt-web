/**
 * 
 * export const api = {
    add: (a, b) => a + b,
    subtract: (a, b) => a - b
  }
 * 
 */


const getConversationMessages = () => {
  // do something
}

const sendApiMessage = () => {
  // do something else
}

export default {
  getConversationMessages,
  sendApiMessage
}